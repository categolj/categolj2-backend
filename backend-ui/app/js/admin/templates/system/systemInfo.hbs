<p>
    <a id="reload-system-info" class="btn btn-info">Get system info</a>
    <a href="#system/threadDump" class="btn btn-info">Get thread dump</a>
</p>


<div class="table-responsive">
    {{#each env}}
        <h2>{{@key}}</h2>
        <table
                class="table table-striped table-bordered table-condensed">
            <tr>
                <th>Name</th>
                <th>Value</th>
            </tr>

            {{#each this}}
                <tr>
                    <td>{{@key}}</td>
                    <td style="word-wrap: break-word;word-break: break-all;">{{this}}</td>
                </tr>
            {{/each}}
        </table>
    {{/each}}
</div>